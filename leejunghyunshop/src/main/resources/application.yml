server:
  port: 8080
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://112.169.196.99/arte?zeroDateTimeBehavior=convertToNull
    username: arte
    password: Arte1234
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
security:
  jwt:
    secret: secret-value

UserController:
  signup: Creates user and returns its JWT token
  signin: Authenticates user and returns its JWT token
