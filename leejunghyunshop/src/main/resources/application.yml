spring:
  datasource:
    url: jdbc:mariadb://bit911215.cebga7nnwvvl.ap-northeast-2.rds.amazonaws.com:3306/bit911215
    driver-class-name: org.mariadb.jdbc.Driver
    username: bit911215
    password: Wjdgus2580
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: create-drop

UserController:
  signup: Creates user and returns its JWT token
  signin: Authenticates user and returns its JWT token